# AGENTS.md

Blog.zip 공통 개발 규칙이다. 사람과 AI 도구 모두 이 문서를 기준으로 작업한다.

특정 AI 하네스에 종속되지 않는 규칙만 여기에 둔다.

## Source of Truth

```text
docs/PRD.md
↓
docs/specs/
↓
docs/decisions/
↓
GitHub Issue
↓
Implementation
```

- 구현과 문서가 충돌하면 **코드를 기준으로 문서를 자동 수정하지 않는다.**
  먼저 어느 쪽이 올바른 요구사항인지 확인하고 잘못된 쪽을 수정한다.
- Spec에 없는 동작을 임의로 추가하지 않는다. 필요하면 Spec을 먼저 수정한다.
- AI가 Spec과 다른 구현을 제안하면 바로 반영하지 않고 요구사항 변경 여부를 먼저 확인한다.
- 특정 하네스가 만든 Plan, Memory, 세션 노트는 팀의 Source of Truth가 아니다.

## 작업 시작 전 읽을 문서

1. `docs/PRD.md` - 제품 범위와 원칙 (특히 7장 제품 원칙, 13장 Business Rules)
2. 해당 기능의 `docs/specs/*.md`
3. 관련 `docs/decisions/*.md`
4. `docs/GROUND_RULES.md` - 브랜치, 커밋, PR 규칙
5. 모듈별 `backend/AGENTS.md`, `frontend/AGENTS.md`

## 제품 규칙 중 구현에서 자주 깨지는 것

- Subscription은 Ownership이 아니다. 구독에 소유권 인증을 요구하지 않는다. (BR-001, BR-002)
- 사용자가 지정한 친구 이름은 **그 사용자에게만** 적용되는 Label이다. 전역 표시명으로 쓰지 않는다. (BR-004, BR-005)
- 친구 이름을 근거로 Blog 소유자를 자동 확정하지 않는다. (BR-010)
- Ownership 인증 전에는 공식 운영자임을 UI/API에서 보장하지 않는다. (BR-008)
- Feed의 모든 콘텐츠는 원본 Blog와 Post 출처를 확인할 수 있어야 한다. (BR-009)
- 사용자 경험에 RSS/Atom 같은 기술 용어를 노출하지 않는다. (P-002)

## 문서 갱신 기준

- 사용자에게 보이는 동작, 비즈니스 규칙, API 계약, 데이터 의미, 예외 정책, 완료 조건이 바뀌면
  같은 PR에서 해당 Spec을 수정한다.
- 내부 리팩터링, 이름 변경, 패키지 이동, 단순 성능 개선은 Spec을 수정하지 않는다.
- 되돌리기 어렵고 다른 개발자도 같은 고민을 할 결정(DB, 인증, 캐싱, Retry/Rate Limit,
  일관성 전략, 시스템 간 통신)은 `docs/decisions/`에 남긴다.

## Git 규칙

- 커밋: `prefix(domain): 한국어 설명`
- 브랜치: `prefix/#이슈번호-description-with-dash`
- `main` 직접 Push 금지. PR은 `dev`로 보낸다.
- Issue 하나 ≒ PR 하나. 관련 없는 변경을 섞지 않는다.
- 커밋 생성은 사용자가 명시적으로 요청할 때만 수행한다.

Prefix: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`, `ci`

## 코드 작업 원칙

- 요청된 범위만 변경한다. 옆에 있는 코드를 함께 정리하지 않는다.
- 기존 파일의 스타일, 네이밍, 라이브러리 선택을 따른다. 새 의존성은 이유를 밝히고 추가한다.
- 외부 입력(블로그 URL, Feed 응답 등)은 신뢰 경계에서 검증한다.
- 에러를 조용히 삼키지 않는다. 실패 상태는 Spec의 예외 처리 정책에 맞춰 다룬다.
- 타입 에러, 린트 경고, 실패 테스트를 억제하거나 삭제하지 않는다.

## 검증

- 변경한 모듈의 빌드와 테스트를 실행하고 결과를 PR에 남긴다.
- 새 기능과 버그 수정에는 테스트를 추가한다. 버그 수정은 가능하면 Regression Test를 남긴다.
- 테스트는 고정 sleep이나 타이밍 운에 의존하지 않는다.
- "통과할 것 같다"는 검증이 아니다. 실행한 결과만 보고한다.

## AI 사용

- AI가 생성한 코드는 작성자가 직접 검토하고 이해한 뒤 PR을 만든다.
- PR 템플릿의 AI 사용 항목을 채운다.
- AI 리뷰 봇 피드백은 실제 문제인지, Spec/설계와 맞는지 판단한 뒤 반영한다.
  사람 리뷰와 충돌하면 팀원 판단을 우선한다.
